required work:
	#done#create web page that takes an input and transfers it to the quandl api to fetch data for that company

	#fetch the data analyse and plot the data

	#show the data in the browser

after work:
	push it to git 

	make a better looking UI

	change the code to remove the error